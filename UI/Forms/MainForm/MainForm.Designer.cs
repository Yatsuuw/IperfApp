namespace IperfApp.UI.Forms.MainForm;

public partial class MainForm
{
    /// <summary>Initialise les composants générés par le designer WinForms.</summary>
    private void InitializeComponent()
    {
        SuspendLayout();
        // Les contrôles réels sont créés dynamiquement dans MainForm.UI.cs → SetupModernUI().
        // Ce fichier ne doit contenir que l'appel SuspendLayout / ResumeLayout
        // pour rester compatible avec une éventuelle ouverture dans le designer.
        AutoScaleDimensions = new SizeF(7F, 15F);
        AutoScaleMode       = AutoScaleMode.Font;
        ClientSize          = new Size(580, 760);
        Name                = "MainForm";
        ResumeLayout(false);
    }
}
